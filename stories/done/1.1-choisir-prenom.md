# Story 1.1 - Choisir son prénom dans la liste

## Epic
Epic 1 : Identification simple

## User Story
**En tant qu'** utilisateur  
**Je veux** choisir mon prénom dans une liste existante  
**Afin de** m'identifier rapidement sans créer de compte

## Contexte
L'application est partagée par toute la famille. Chaque personne doit pouvoir s'identifier simplement en choisissant son prénom. Pas de mot de passe, pas d'email - juste un prénom.

## Critères d'Acceptation

- [ ] Un écran d'identification s'affiche au lancement si l'utilisateur n'est pas déjà identifié
- [ ] Une liste déroulante (dropdown) affiche tous les prénoms existants dans la base
- [ ] L'utilisateur peut sélectionner son prénom dans la liste
- [ ] Après sélection, l'utilisateur est redirigé vers la galerie principale
- [ ] Si la liste est vide, afficher un message invitant à ajouter un prénom (voir story 1.2)

## Éléments Techniques

### Base de données
```sql
-- Table users déjà définie dans CONTEXT.md
SELECT id, name, role FROM users ORDER BY name;
```

### API Endpoint
```
GET /api/users
→ Retourne la liste des utilisateurs [{id, name, role}]
```

### Composant Frontend
- Page/écran : `IdentificationScreen` ou `LoginPage`
- Dropdown avec les prénoms
- Bouton "C'est moi" ou validation automatique au clic

### Stockage Local
- Sauvegarder `user_id` dans `localStorage` après sélection
- Vérifier `localStorage` au chargement pour skip l'identification

## Maquette Simplifiée

```
┌─────────────────────────────┐
│                             │
│    🦁 Héritage Partagé      │
│                             │
│    Qui êtes-vous ?          │
│                             │
│    ┌─────────────────────┐  │
│    │ Choisir un prénom ▼ │  │
│    └─────────────────────┘  │
│                             │
│    Pas dans la liste ?      │
│    [Ajouter mon prénom]     │
│                             │
└─────────────────────────────┘
```

## Dépendances
- Aucune (première story à implémenter)

## Stories Liées
- 1.2 : Ajouter son prénom si absent
- 1.3 : Indiquer son rôle (parent/enfant)
- 1.4 : Mémorisation sur l'appareil
